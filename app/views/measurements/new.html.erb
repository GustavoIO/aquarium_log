<h1>New Measurement</h1>
<p class="text-muted">Adding measurement to: <strong><%= @aquarium.name %></strong></p>

<%= render "form", measurement: @measurement %>

<div class="mt-3">
  <%= link_to "Back to aquarium", @aquarium, class: "btn btn-outline-secondary" %>
</div>
